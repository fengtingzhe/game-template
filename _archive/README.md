# _archive：历史参考区

本目录用于临时保存旧文档、旧规则或迁移前资料。

---

## 读取规则

AI 默认禁止读取 `_archive/`。

只有当 `TASK.md` 的“必须读取的额外文件”明确列出 `_archive/` 中的具体文件时，AI 才能读取。

---

## 原则

```text
_archive/ 只做历史参考。
_archive/ 不作为当前规则来源。
_archive/ 不参与默认上下文。
```
